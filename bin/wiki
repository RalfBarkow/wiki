#!/usr/bin/env sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
BIN_PATH="$ROOT_DIR/result/bin/wiki"

if [ ! -x "$BIN_PATH" ]; then
  echo "ERROR: Nix build output not found at $BIN_PATH" >&2
  echo "Run: nix build .#wiki" >&2
  exit 1
fi

exec "$BIN_PATH" "$@"
